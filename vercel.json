{
  "buildCommand": "cd frontend && npm install --legacy-peer-deps && npx react-scripts build",
  "outputDirectory": "frontend/build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
